# OS / editor cruft
.DS_Store
Thumbs.db
desktop.ini
*.swp
*~

# Local preview helper logs (keep serve.cjs and launch.json — they're useful)
*.log

# Personal Claude Code config (keep serve.cjs / launch.json, exclude local settings)
.claude/settings.local.json
